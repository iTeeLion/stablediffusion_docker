version: '3.8'
services:
  sdwebui:
    container_name: sdwebui
    restart: unless-stopped
    build:
      context: ''
      args:
        ARG1: 'arg1'
    ports:
      - '8888:8888'
    deploy:
      resources:
        reservations:
          devices:
              - driver: nvidia
                device_ids: ['0']
                capabilities: [compute, utility]
